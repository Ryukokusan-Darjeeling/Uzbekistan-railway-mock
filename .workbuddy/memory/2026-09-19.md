# 2026-09-19

- 用户提出架构演进方向：将 advisor.js 中「单次调用 Groq API + 正则解析自然语言」的供货调整方式，升级为「智能体闭环动态调整」（感知→规划→护栏→执行→反馈）。
- 已给出改造建议：JSON 结构化输出替代正则解析、决策记忆反馈环、护栏钳制与月限幅、本地规则引擎保留为降级方案、API Key 移出浏览器。
- 后续若实施，切入点为 advisor.js（CargoAdvisor）与 app.js 的 getAdvice()/aiToggle 流程。

## DeepSeek 智能体改造（已实施）
- 用户因 Groq 免费额度不足改用 DeepSeek API，参考官方文档完成改造：
  - advisor.js：重写为 DeepSeek 智能体（https://api.deepseek.com/chat/completions, model deepseek-flash, response_format json_object）；删除正则解析；新增护栏（钳制 0.75~1.30、单月限幅 ±0.10 相对前次决策）、决策记忆（localStorage `cku_advisor_memory`，最多 12 期）、空 content 重试一次、失败降级本地引擎。
  - simulation.js：revenueHistory 增加 adjustments 快照。
  - app.js：新增 _syncAdvisorMemory() 反馈闭环（推进月/快进 6 月/获取建议前同步）；reset 清空记忆；语言切换只对 isLocal 建议重新生成。
  - i18n.js / index.html / README.md：Groq/Llama 文案全部替换为 DeepSeek；advisor.prompt.system 改为 JSON 输出契约（含 json 字样，DeepSeek JSON Output 硬性要求）。
- 验证：4 个 JS 文件 node --check 通过；10 项护栏/记忆/解析冒烟测试全部通过（测试脚本在 %TEMP%\cku_advisor_smoke.js）。
- 注意：Edit 工具本会话偶发"假成功"，重要编辑后需用 Grep/Read 复核落盘。

## 本地 API Key 配置（已实施）
- 用户要求 DeepSeek Key 配置在本地、不再网页输入：新建 `js/config.local.js`（window.DEEPSEEK_API_KEY），新建 `.gitignore` 排除该文件；index.html 在 advisor.js 前加载它；advisor.js 构造时优先读取本地 Key；app.js init 时回填输入框掩码显示。

## 多智能体流水线（已实施）
- 用户确认做多智能体，延迟预算 30 秒/棒。advisor.js 重构为双模式编排器（single/multi 可切换，默认 multi）：
  - 三角色流水线：市场分析员→调度规划官→风险审查官，各角色独立系统提示词（i18n 键 advisor.prompt.analyst/planner/risk）与 JSON 契约校验。
  - _callDeepSeek 加 AbortController 30s 超时；每棒重试 1 次；降级策略按棒独立：分析员失败→规划官用原始数据；规划官失败→整体降级本地引擎；审查官失败→直接采纳草案。
  - 风险审查官 verdict=approve/modify/reject，最终乘数经代码护栏强制执行；审查意见附加到策略要点；pipelineStats 统计干预次数。
  - UI：index.html 加单/多智能体切换 select；app.js 经 onStageUpdate 回调渲染三棒接力时间线（advisor.css 新增样式）；建议卡片显示流水线徽标。
- 验证：14 项多智能体冒烟测试 + 10 项单智能体回归测试全部通过。测试脚本在 %TEMP%\cku_multiagent_smoke.js、cku_advisor_smoke.js。

## 错误 UI 与超时放宽（已实施）
- 单棒超时 30s→60s（STAGE_TIMEOUT_MS=60000）。
- API 失败/超时不再静默降级本地引擎：advisor.getAdvice 抛出归一化错误 err.advisorError（type: timeout/http/parse/unknown）；app.js 渲染 .agent-error 错误卡片（标题+分类说明+原始 detail+按钮）。
- 按钮：「🔄 重试」重新走完整流程；「使用本地引擎」由用户主动降级（复用新抽取的 app._applyAdvice(advice, resultEl) 方法）。
- i18n 新增 ai.error.title/timeout/http/parse/unknown/retry/useLocal（中英）；advisor.css 新增 .agent-error 样式。
- 验证：15 项多智能体测试（含错误归一化）+10 项回归全过。

## 2.0 架构方向（规划阶段，未改代码）
- 用户规划 2.0 版本：架空世界中「清朝→俄国→奥斯曼→奥地利」铁路贸易线，智能体从"协作求最优"转为"各自扮演自私地方官员，谋求任内个人利益最大化"；流官三年一换、可花钱打点安插亲戚；各地制度不同、每地多官员。
- 已联网查证历史制度原型：清流官制（三年任期/回避/捐纳/考成）、奥斯曼包税制（iltizam 1-3 年拍卖 / malikane 1695 终身世袭）、俄国职级表、哈布斯堡开明专制重商主义。
- 核心结论：2.0 重心是"制度引擎（手写规则）"，LLM 降级为角色策略生成器；官员目标函数=任内个人综合收益（财富/政治资本/家族延续/安全加权）；贸易线=公共资源池，过境博弈形成兴衰循环。
- 已产出架构文档：docs/CKU-Railway-2.0-Architecture.md（含分层架构、数据模型、回合结构、分阶段路线图、开放问题）。
- 后续修正：用户要求地名国名用真实名称，已把架空称谓全部替换——清朝（汉口/张家口/恰克图）、俄国（伊尔库茨克/圣彼得堡）、奥斯曼帝国（阿勒颇/君士坦丁堡）、奥地利哈布斯堡（维也纳/的里雅斯特）；文档标题改为"真实地理·写实制度"。

## 2.0 需求细化（进行中）
- 用户要求：①细化需求书 ②各地城市要多、路线要能串通 ③加入商人作为额外变量 ④UI 改为契合时代（19 世纪欧亚贸易）风格、禁止 emoji ⑤帮忙找 UI 背景资源。
- 已联网查证真实城市节点与商路：万里茶道（武夷山→汉口→张家口→库伦→恰克图→伊尔库茨克→新西伯利亚→叶卡捷琳堡→喀山→莫斯科→圣彼得堡）；黑海/海路（敖德萨、苏伊士、君士坦丁堡、的里雅斯特、维也纳多瑙河航运）。
- 视觉风格定为「复古地图/羊皮纸 + 黄铜蒸汽工业」：配色（羊皮纸 #F4E8C1、黄铜 #B87333、氧化铜 verdigris #43B3AE、深棕）、字体（Cinzel/Playfair Display 衬线 + 手写/打字机风格）、齿轮罗盘边框、无 emoji，图标改用 CSS/SVG 几何图形与复古线描。
- 已重写 docs/CKU-Railway-2.0-Architecture.md 为详细需求书：真实城市节点网络（清：武夷山/安化/羊楼洞/汉口/张家口/库伦/恰克图；俄：伊尔库茨克/乌兰乌德/新西伯利亚/叶卡捷琳堡/喀山/莫斯科/圣彼得堡；海路：上海/海参崴/苏伊士/敖德萨；奥斯曼：阿勒颇/君士坦丁堡；奥：维也纳/的里雅斯特）+ 商人智能体（选路/行贿/走私/勾结/商帮兴衰）+ 制度引擎硬约束 + UI 复古重构方案 + 5 个开放问题。
- 补充：用户要求官员官位+姓名符合民族特色（尤其清朝别全汉族）。已新增 §2.4 官员名册——清朝分满缺/汉缺/蒙缺（恰克图办事司员、库伦办事大臣=满缺、帮办大臣=蒙缺、汉黄德道等=汉缺，人名如富察·讷尔经额、桑寨多尔济、爱新觉罗·舒明）；俄国用斯拉夫姓名+总督/省长职衔；奥斯曼用突厥-阿拉伯姓名+帕夏/贝伊/卡迪；奥地利用德语姓名+贵族封号。官员模型新增 ethnicity 字段，换官按官缺从对应民族候选池抽取。

## README 同步（已实施）
- 按用户要求更新 README：标题改为 Multi-Agent LLM Dispatch Pipeline；Features §2 重写（三角色流水线、单智能体模式保留、60s 超时与按棒降级、代码护栏、错误卡片、决策记忆、流水线时间线）；新增「DeepSeek API Key」配置说明（config.local.js）与「Multi-Agent Architecture」ASCII 架构图小节。
